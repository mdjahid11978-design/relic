<!--
  收件箱 / Inbox
  导入缓冲区：把义体的旧记忆文件放在这里，让义体自动转换。
  Import buffer: place agent's old memory files here for auto-conversion.
-->

# 收件箱 / Inbox

> 📭 **目录现在是空的，这是正常的。**
> 只有当你需要从其他 AI Agent 导入旧记忆时才用得到这里。把旧记忆文件放进来，告诉 AI 转换就行。

然后告诉义体："请阅读 INBOX/ 里的文件，按 PROTOCOL.md 的格式转换到 MEMORY.md"
Then tell the agent: "Read files in INBOX/, convert to MEMORY.md per PROTOCOL.md"

用户确认导入无误后，此目录中的源文件会被删除。
Source files will be deleted after user confirms import is correct.
